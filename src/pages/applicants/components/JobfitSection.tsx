import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

const JobfitSection = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle className="uppercase">Job Fit</CardTitle>
      </CardHeader>
      <CardContent></CardContent>
    </Card>
  );
};

export default JobfitSection;
